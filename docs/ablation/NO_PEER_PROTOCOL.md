# 他SDK情報をすべて非表示にする追加実験

2026-09-16。元のコード非表示条件を対照に、残っていた他SDK情報をすべて外す。

- outcomes：従来のコード非表示と同じ。他SDKの問題文・最新合否/出力/エラー・評価履歴は見える。
- none：latest peer evidenceは空配列。履歴は自SDKの行だけ。他SDKの当該問題情報は見えない。
- 両条件とも自身の診断を保持し、他SDKの診断は非共有。元の停滞処理・モデル・最大5修正は維持。
- 元の一般ポリシーに他SDKの名前・利用指示は残す。試行固有の情報を与えない条件を切り分ける。
- 初回126件だけ共通の保存実測を使用。それ以降は全条件で新しい生成を行う。
- 2条件×各2回×126ケースを、途中成績によらずすべて完了する。
- 過去の107/107を主対照にせず、outcomesも2回新たに実行する。
- 平均合格数差（outcomes−none）と各回の差、全ケースの合否、コストを報告する。
- 3SDKと2回を平均した42問題単位の対応差をbootstrap20000回、seed2026091602。
- 両回で差が正、かつ95%区間下限>0の場合だけ一貫した正の効果を支持したと扱う。
- 回答・コード・使用量が返らない厳密なcapacity失敗はログを保存し、同じ候補枠を同一入力で1回だけ再送可能。
  完了応答のtransport通知は生ログから同じ応答を復元する。失敗バックエンド呼出しは別集計し、その他のエラーは表面化する。

実行前設定・ソースhash・起動順は `results/ablation/no_peer_registry.json`。
監査ではhost側のsource_promptでなく、実際に送信したmodel/prompt.txtに他SDK情報がないことを確認する。
同一ベンチマーク上の開発比較であり、未見評価やトークン数を揃えた比較ではない。
